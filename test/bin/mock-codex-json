#!/usr/bin/env node
// POSIX launcher — logic lives in mock-codex-json.js (shared with
// mock-codex-json.cmd for Windows, since cmd.exe can't run a shebang script
// directly).
require("./mock-codex-json.js");
